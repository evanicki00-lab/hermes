# Server Access

Default SSH target:

```powershell
ssh -i "$HOME\.ssh\hermes-vds\78.17.192.180_ed25519" -p 22 root@78.17.192.180
```

Do not commit SSH private keys. Do not disable password login or change SSH hardening from this repo unless there is a separate approved task.
